<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingLeft="32dp"
    android:paddingRight="32dp"
    android:background="#F4F5FA">

    <!-- brand badge -->
    <TextView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="@drawable/bg_badge"
        android:gravity="center"
        android:text="৳"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SMS পেমেন্ট ফরওয়ার্ডার"
        android:textSize="21sp"
        android:textStyle="bold"
        android:textColor="#1B1B2F"
        android:letterSpacing="0.0"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="bKash/Nagad পেমেন্ট SMS এলে স্বয়ংক্রিয়ভাবে যাচাই হবে"
        android:textSize="13sp"
        android:textColor="#7A7E92"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="48dp" />

    <!-- main toggle button -->
    <Button
        android:id="@+id/btnToggle"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="START"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:textAllCaps="false"
        android:letterSpacing="0.05"
        android:background="@drawable/btn_state"
        android:stateListAnimator="@null"
        android:elevation="0dp" />

    <!-- status pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_status_pill"
        android:padding="16dp"
        android:layout_marginTop="20dp">

        <TextView
            android:id="@+id/tvState"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="অবস্থা: বন্ধ আছে"
            android:textSize="13.5sp"
            android:textStyle="bold"
            android:textColor="#1B1B2F" />

    </LinearLayout>

    <!-- last activity card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_card"
        android:padding="16dp"
        android:layout_marginTop="12dp">

        <TextView
            android:id="@+id/tvLast"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="এখনো কোনো SMS আসেনি"
            android:textSize="12sp"
            android:textColor="#7A7E92"
            android:gravity="center"
            android:lineSpacingMultiplier="1.3" />

    </LinearLayout>

</LinearLayout>
